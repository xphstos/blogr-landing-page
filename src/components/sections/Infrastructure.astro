---

---

<section>
  <figure>
    <img src="/illustration-phones.svg" width="552" height="579" alt="" />
  </figure>
  <header>
    <h2>State of the Art Infrastructure</h2>
    <p>
      With reliability and speed in mind, worldwide data centers provide the
      backbone for ultra-fast connectivity. This ensures your site will load
      instantly, no matter where your readers are, keeping your site
      competitive.
    </p>
  </header>
</section>

<style scoped>
  section {
    display: grid;
    justify-items: center;
    padding-inline: container();
    padding-block: 0 6rem;
    isolation: isolate;
    color: var(--white);
    position: relative;
    text-align: center;
  }

  section::before {
    content: "";
    position: absolute;
    inset: 10vh 0 0 0;
    background:
      url("/bg-pattern-circles-mobile.svg") no-repeat center top / 100% auto,
      linear-gradient(
        to bottom,
        var(--very-dark-gray-blue),
        var(--very-dark-desaturated-blue)
      );
    border-start-end-radius: fluid(5rem, 6.25rem);
    border-end-start-radius: fluid(5rem, 6.25rem);
    z-index: -1;
  }

  @media (width >= 1024px) {
    section {
      text-align: left;
      padding-block: 0;
      place-items: center;
      grid-template-columns: 1fr min(34rem, 100%);
      gap: 0 fluid(1.5rem, 3rem);
    }

    section::before {
      background:
        url("/bg-pattern-circles-desktop.svg") no-repeat left top / 800px auto,
        linear-gradient(
          to right,
          var(--very-dark-gray-blue),
          var(--very-dark-desaturated-blue)
        );
      inset: calc(3rem + 1rem) 0 calc(3rem + 3.25rem) 0;
    }
  }

  figure {
    margin: 0;
  }

  @media (width < 1024px) {
    header {
      width: min(40rem, 100%);
    }
  }

  h2 {
    font-size: fluid(2.25rem, 2.5rem);
    font-weight: 500;
    margin-block-end: fluid(1rem, 1.75rem);
    letter-spacing: -0.03em;
  }

  p {
    line-height: 1.75;
    color: var(--grayish-blue);
    text-wrap: balance;
  }
</style>
